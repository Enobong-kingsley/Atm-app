<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditText
        android:id="@+id/withdraw_dialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Amount !"
        android:inputType="number"
        android:gravity="center"
        android:padding="10dp"
        android:textSize="18sp"
        android:background="@drawable/display_drawable"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="40dp"/>

    <TextView
        android:id="@+id/ok_button"
        android:layout_width="150dp"
        android:layout_height="70dp"
        android:layout_marginTop="20dp"
        android:text="Click ok complete transaction now !"
        android:gravity="center"
        android:textStyle="bold"
        android:textSize="14sp"
        android:textColor="@color/colorWhite"
        android:background="@color/colorAccent"
        app:layout_constraintEnd_toEndOf="@+id/withdraw_dialog"
        app:layout_constraintStart_toStartOf="@+id/withdraw_dialog"
        app:layout_constraintTop_toBottomOf="@+id/withdraw_dialog" />



</androidx.constraintlayout.widget.ConstraintLayout>